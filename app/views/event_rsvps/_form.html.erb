<%= form_for(@event_rsvp) do |f| %>
  <% if @event_rsvp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_rsvp.errors.count, "error") %> prohibited this event_rsvp from being saved:</h2>

      <ul>
      <% @event_rsvp.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :probability %><br />
    <%= f.text_area :probability %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
